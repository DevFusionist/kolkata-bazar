# Neon (or any PostgreSQL) connection string. Use the "pooled" connection string from Neon dashboard.
# Without this, the app uses in-memory storage (no persistence).
# If you see an SSL warning, use sslmode=verify-full in the URL (the app normalizes require→verify-full).
DATABASE_URL='postgresql://user:password@host/db?sslmode=verify-full'

# Twilio Verify for login OTP (https://www.twilio.com/docs/verify/api).
# Create a Verify service in Twilio Console > Verify > Services. Without these, login OTP returns 503.
# TWILIO_ACCOUNT_SID=your_account_sid
# TWILIO_AUTH_TOKEN=your_auth_token
# TWILIO_VERIFY_SERVICE_SID=your_verify_service_sid

# Optional: session cookie (at least 16 chars). When set, returning users stay signed in without OTP.
# SESSION_SECRET=your_random_secret_at_least_16_chars

# Optional: comma-separated list of allowed CORS origins (default: reflect request origin).
# Example: http://localhost:5015,https://myapp.com
# CORS_ORIGIN=

# Server port (default 5014)
PORT=5014
